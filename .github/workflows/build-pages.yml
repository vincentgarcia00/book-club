on:
  push:
    branches:
      - master
      - main

jobs:
  build_pages:
    runs-on: ubuntu-latest
    name: Builds Github pages and updates gh-pages branch
    steps:
  - uses: actions/checkout@v3
  - uses: actions/setup-node@v3
    with:
      node-version: 18
  - run: npm deploy
